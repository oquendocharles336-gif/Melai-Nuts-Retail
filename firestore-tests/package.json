{
  "name": "melai-nuts-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-melai \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.0.0"
  }
}
