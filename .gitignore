# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Widget Preview related
.widget_preview/

# ---------------------------------------------------------------------------
# Secrets / private credentials — NEVER commit these.
# Firebase API keys are injected at build time (env/firebase.json via
# --dart-define-from-file) and google-services.json is kept local. Only the
# *.example templates are tracked. See SECURITY.md.
# ---------------------------------------------------------------------------
/env/*
!/env/*.example.json
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
macos/Runner/GoogleService-Info.plist
.env
.env.*
!.env.example
*.env
secrets/
# Firebase Admin SDK / Google Cloud service-account keys
*-adminsdk-*.json
*firebase-adminsdk*.json
service-account*.json
serviceAccount*.json
serviceaccountkey*.json
# Android signing material
*.jks
*.keystore
key.properties
android/key.properties
# Private keys / certificates
*.pem
*.p12
*.pfx
*.key
# Firebase CLI local state & logs
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
# Rules-test tooling
node_modules/
